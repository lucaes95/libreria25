# Libreria – Client/Server
Breve descrizione, come compilare server e client, come eseguire in locale, credenziali di test.

## Build
- Server (C): `cd server-c && make` oppure `cmake .. && make`
- Client: istruzioni per Java/Kotlin/C

## Esecuzione
- Server: `./server [porta]`
- Client: `./client ...` o `java -jar client.jar ...`

## Struttura
spiega cartelle, DB/file, limiti noti, TODO

